# Audio System

Application React minimale dediee au systeme audio.

## Demarrage

```bash
npm install
npm run dev
```

## Scripts

- `npm run dev` lance l'application en local
- `npm run build` verifie TypeScript puis genere le build production
- `npm run lint` lance ESLint sur le projet

## Etat actuel

- l'entree applicative affiche `AmbientDashboard`
- l'application ne depend pas du mock visuel Vite
- les runtimes audio bases sur le routing ne sont pas branches
